<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorPrimary">@color/textColorPrimary</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="label_style">
        <item name="android:fontFamily">"@font/roboto"</item>
        <item name="android:textSize">@dimen/label_text_size</item>
    </style>

    <style name="text_style">
        <item name="android:fontFamily">"@font/roboto"</item>
        <item name="android:textSize">@dimen/text_size</item>
    </style>

    <style name="button_text_style">
        <item name="android:fontFamily">"@font/roboto"</item>
        <item name="android:textSize">@dimen/button_text_size</item>
        <item name="android:padding">@dimen/padding</item>
    </style>

    <style name="edit_short_style">
        <item name="android:fontFamily">"@font/roboto"</item>
        <item name="android:textSize">@dimen/text_size</item>
        <item name="android:padding">@dimen/padding</item>
        <item name="android:layout_margin">@dimen/margin</item>
        <item name="android:textColor">@color/editFieldsTextColor</item>
        <item name="android:minHeight">@dimen/edit_short_min_height</item>
    </style>

    <style name="edit_long_style">
        <item name="android:fontFamily">"@font/roboto"</item>
        <item name="android:textSize">@dimen/text_size</item>
        <item name="android:padding">@dimen/padding</item>
        <item name="android:layout_margin">@dimen/margin</item>
        <item name="android:textColor">@color/editFieldsTextColor</item>
        <item name="android:minHeight">@dimen/edit_long_min_height</item>
    </style>

</resources>
